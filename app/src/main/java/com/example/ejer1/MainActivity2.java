package com.example.ejer1;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.TextView;

public class MainActivity2 extends AppCompatActivity {
    TextView txtTitle2;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        /*setContentView(R.layout.activity_main2);
        String nombre = getIntent().getExtras().getString("Usuario");
        Integer nr1 = getIntent().getExtras().getInt("nr1");
        Integer nr2 = getIntent().getExtras().getInt("nr2");
        int suma = nr1 + nr2;
        txtTitle2=findViewById(R.id.txtTitle2);
        txtTitle2.setText(nombre);
        txtTitle2.setText(suma);*/
    }






}